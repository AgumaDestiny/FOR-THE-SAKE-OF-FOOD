{
    "version": 2,
    "buildCommand":"npx prisma generate; npm run dev",
    "builds": [
      {
        "src": "index.ts", 
        "use": "@vercel/node"
      }    
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "index.ts"
      }
    ]
  }